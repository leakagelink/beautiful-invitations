# Roadmap — My Invitation

## Done
- 1 minute video option (15s / 30s / 60s, default 60s) — no more short-only clips
- Admin Panel (`/admin`, linked from Profile): add template (background image, occasion, colours, photo/video options, default names/date/message/venue in English + Telugu), delete template
- Added templates appear in Explore, occasion pages, Search and the editor
- Name/date/venue fields have character limits + counters; long names auto-shrink in the card so the design never breaks

## Open
- Login / sign-up (name, auto email verify, auto login) — blocked: workspace credits needed for Lovable Cloud
- Admin templates are stored on this device only; sharing them with all users needs Cloud
- Optional: more built-in wedding designs; multi-photo slots; guest list & RSVP
